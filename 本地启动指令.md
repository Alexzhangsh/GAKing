# 金角大王 — 本地开发启动指令

> 项目路径：`/Users/alexzhang/Documents/Work/Projects/Products/金角大王/GAKing-Coding`

---

## 环境说明

| 服务 | 端口 | 说明 |
|------|------|------|
| Redis | 6379 | 本地 brew 安装，开机自启，无需手动启动 |
| MySQL | 3306 | 使用远程开发库 `121.37.173.132`，无需本地部署 |
| 后端 FastAPI | 3001 | 需手动启动 |
| 管理后台 | 8080 | 需手动启动 |
| 小程序 H5 预览 | 5173 | 需手动启动 |

---

## 🚀 推荐：安全重启版命令（直接复制粘贴）

每个命令都会**先杀掉占用端口的旧进程，再启动新的**，不用手动找 PID。

### 终端1 — 启动后端

```bash
/Users/alexzhang/Documents/Work/Projects/Products/金角大王/GAKing-Coding/scripts/restart_backend.sh
```

验证：`curl http://localhost:3001/healthz` → 返回 `{"status": "ok"}`

### 终端2 — 启动管理后台

```bash
/Users/alexzhang/Documents/Work/Projects/Products/金角大王/GAKing-Coding/scripts/restart_admin.sh
```

浏览器访问：http://localhost:8080
默认账号：`admin / admin@12345`

### 终端3 — 启动小程序 H5 预览

```bash
/Users/alexzhang/Documents/Work/Projects/Products/金角大王/GAKing-Coding/scripts/restart_miniprogram.sh
```

浏览器访问：http://localhost:5173

### 一键停止所有服务

```bash
/Users/alexzhang/Documents/Work/Projects/Products/金角大王/GAKing-Coding/scripts/stop_all_local.sh
```

---

## 手动启动版（不推荐）

如果你想手动控制每个步骤，可以用下面的命令：

### 启动后端

```bash
cd /Users/alexzhang/Documents/Work/Projects/Products/金角大王/GAKing-Coding/backend
source .venv/bin/activate && PYTHONPATH="." python src/main.py
```

### 启动管理后台（新开终端）

```bash
cd /Users/alexzhang/Documents/Work/Projects/Products/金角大王/GAKing-Coding/admin
unset NPM_CONFIG_PREFIX && source ~/.nvm/nvm.sh && nvm use v18.20.8 && npm run dev
```

### 启动小程序 H5 预览（新开终端）

```bash
cd /Users/alexzhang/Documents/Work/Projects/Products/金角大王/GAKing-Coding/miniprogram
unset NPM_CONFIG_PREFIX && source ~/.nvm/nvm.sh && nvm use v18.20.8 && npm run dev:h5
```

> 微信小程序调试：执行 `npm run dev:mp-weixin`，用微信开发者工具打开 `dist/dev/mp-weixin` 目录。

---

## 常用验证

```bash
# 检查后端
curl http://localhost:3001/healthz

# 检查 Redis
redis-cli ping     # 返回 PONG

# 检查端口占用
lsof -i :3001      # 后端
lsof -i :8080      # 管理后台
lsof -i :5173      # 小程序 H5
lsof -i :6379      # Redis
```
