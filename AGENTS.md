# AGENTS.md — 金角大王项目启动指令

> 任何 Agent（豆包 / Trae / 其他）新会话的**唯一启动入口**：按下面顺序读档，再开始干活。
> 本文件与 `docs/发布协作/` 文件共同构成协作机制（M07-5，小脉养车双 Agent 模式迁移）。

## 新会话启动顺序（必读，按序）

1. `docs/发布协作/发布步骤清单.md` —— **批次状态唯一归口**：先看当前流转点，只做自己角色该处理的状态。
2. `docs/发布协作/发布操作文档.md` —— 执行手册：批次生命周期、门禁、命令、交接单模板。
3. `docs/发布协作/生产发布清单.md` —— 动生产前必读：铁律、资金红线、回滚预案。
4. 涉及环境/连接时：`docs/发布协作/部署与发布手册.md` + `docs/发布协作/线上数据库访问备忘.md`。
5. 体系总纲：`../Documents/07_运维部署/M07-3-生产环境运维流程.md`、`M07-4-多Agent开发发布运维流程.md`（Documents 目录在 git 外，按需读）。

## 角色与职责

| 角色 | 担任者 | 职责 | 本机制落点 |
|---|---|---|---|
| PM | 豆包（Alex 指令下发） | 任务拆解/编号、查重、Gate D/T/P 判定、甘特图 | 发布步骤清单状态判定 |
| Dev（Producer） | Trae 多实例 | 编码、自测、本地构建、**构建即登记**交接单 | 交接单 + 批次清单「变更内容」段 |
| QA | 豆包/脚本 | 回归/冒烟/联调、缺陷台账 | 交接单「测试结论」段 |
| Release（Executor） | 运维脚本 + Alex 审核 | 读单→硬闸门→备份/发布/回滚→台账 | 交接单「核对/发布」段 |
| Monitor | 豆包 | 留观 30 分钟、日志/对账观察 | 交接单「留观结论」段 |

## 硬约束（违反即打回）

1. **文件为媒、单写者、闭环标、硬闸门**（详见发布操作文档 §一）。
2. **构建即登记**：本地构建成功必须当场登记批次+交接单，禁止"构建了但不说"。
3. **发布前探测未登记改动**：`git status --short` + 产物时间戳检查；未登记先补占位批次，禁止逆向猜意图。
4. **任务编号严禁自创**：沿用清单既有编号（S/B/F/M 系列）；发布单号 `REL-YYYYMMDD-NNN`。
5. **禁硬编码密钥**：密码/AK-SK/Webhook 一律从 `.env.*` 或配置读取，git 禁止提交任何 `.env*`、`*.pem`。
6. **代码红线**：Black 格式、类型注解、Redis 键 `gak:` 前缀、IO 捕获异常、全局 Logger、CPU 密集入线程池（M07-4 §4.3）。
7. **资金红线**：对账 0 容忍、提现禁自动重试、资金变更发布前手动对账（生产发布清单 §三）。

## 常用命令（详见发布操作文档 §四）

```bash
bash scripts/release_check.sh <交接单>          # 硬闸门，exit 0 才可发布
bash scripts/check_unregistered.sh              # 未登记改动探测（发布前必跑）
bash scripts/deploy_backend.sh test             # 测试环境部署
python3 scripts/s06_full_chain_test.py          # 全链路验证
bash scripts/health_check.sh http://localhost:3003   # 健康检查（服务器）
```

## Dev 会话收尾检查（构建即登记的自动执行点）

**提交约定（每次修改成功后自动执行，任何新窗口 Agent 必守）**：
0. **修改成功即提交 git**：本地自测通过后立即 `git add <改动文件>` + `git commit -m "<功能名>: <简要说明>"`（如 `商品管理重构: 新增价格与奖励字段`）。
   - **提交完整性**：依赖变更必须连 `package-lock.json`/锁文件一起提交；涉及数据库必须连 alembic 迁移文件；删除文件用 `git add -A` 确保删除也进提交。
   - **一个任务一个提交**：禁止把无关改动混入同一提交；禁止用"初始化/临时/update"等无信息提交信息。
   - **不 push**：push 由发布侧（Release）执行，开发窗口只 commit。
   - 提交后跑 `git status --short` 确认工作区干净，才算本次修改真正完成。

每次 Dev（Trae/豆包）结束编码前，必须依次执行：
1. `git status --short` —— 确认本次改动范围
2. 若本地构建成功或代码已提交：**当场登记批次 + 交接单**（`docs/发布协作/交接单/REL-*-<任务编号>.md`），更新 `发布步骤清单.md` 状态为「待核对」
3. 登记后跑 `bash scripts/check_unregistered.sh` 自检 —— 输出不再有本次任务相关 ❌ 才算收尾完成
4. 收尾检查结果（登记时间/交接单路径）随交付说明一并告知 PM

## 项目速览

- 金角大王 = CPS 返利小程序：后端 FastAPI（Docker 3003）+ 管理后台（8080）+ 小程序；华为云 ECS + 内网 RDS MySQL + OBS + Redis。
- 生产服务器 `124.70.196.241`；代码 git 仓库（GitHub `Alexzhangsh/GAKing`）。
- 详见 `docs/金角大王一期提现迭代交付登记.md` 等交付文档。
